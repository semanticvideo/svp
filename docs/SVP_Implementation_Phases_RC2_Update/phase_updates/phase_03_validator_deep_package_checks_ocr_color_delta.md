# Phase 03 Delta: Deep OCR and Color Validation

Implement schema validation, ID/reference validation, bounding-box validation, color-bucket validation, percentage-total validation, provenance validation, and index/package consistency checks for `/text/` and `/colors/`.

Add tests for all new OCR and color validation codes.
