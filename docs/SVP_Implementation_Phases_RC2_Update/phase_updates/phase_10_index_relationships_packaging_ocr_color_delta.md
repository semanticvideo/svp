# Phase 10 Delta: Text and Color Index Integration

Add SQLite tables and query paths for text regions, recognized text, normalized text, numeric values, color observations, bucket coverage, target references, and text-region foreground/background color lookups.

Update `index_manifest.logical_rows_blake3` procedure to include the new OCR/color tables.
